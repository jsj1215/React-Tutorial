import React from 'react';

const Main = (props) => {
    return (
        <>
            <p>[선물세트]{props.name} {props.price} {props.discount}</p>
                   
            
        </>
    );
};

export default Main;